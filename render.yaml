services:
  - type: web
    name: engageai-backend
    plan: free
    env: node
    buildCommand: npm install
    startCommand: node index.js
    envVars:
      - key: NODE_ENV
        value: production
